package httpadapter

// TODO:
// - Implement HTTP handlers for:
//   - PUT /api/files/{name} (raw bytes, 204)
//   - GET /api/files/{name} (raw bytes, 200)
// - Enforce max upload size in HTTP layer (413).
// - Map domain/usecase errors to OpenAPI ErrorResponse + proper status.

